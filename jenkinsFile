pipeline{
    agent any
    stages{
        stage("Git Checkout"){
            steps("SCM"){
                git 'https://github.com/sri476/pipeline.git'
            }
        }
        stage("maven build"){
            steps("Maven"){
                bat 'mvn clean package'
            }
        
        }
    }
}
